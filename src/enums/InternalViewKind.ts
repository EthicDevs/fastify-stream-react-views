/**
 * An enum that define the different named views used internally so
 * they can be overridden in a type-safe way by users.
 **/
export enum InternalViewKind {
  INTERNAL_ERROR_VIEW = "InternalErrorView",
  NOT_FOUND_ERROR_VIEW = "NotFoundErrorView",
}
